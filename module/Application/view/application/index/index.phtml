

<div class='card'>
    <?php if($game):?>

  <?php if($isComplete && !$isRegister) : ?>


                  <h4>La prochaine partie est complète!</h4>
                  <hr>
                  <div class='row'>
                    <div class="col-12 text-center">
                        <p>Consulter de nouveau cette page plus tard pour vérifier si des places se sont libérées ! </p>
                    </div>

                  </div>

<?php if (false): ?>
              <?php if(!$isInWaitingList) : ?>
                
                  <div class='row'>
                    <div class="col-12 text-center">
                        <p>Vous pouvez vous inscrire en file d'attente <br> et recevoir un message si une place se libère </p>
                    </div>
                      <div class="col-12 text-center">
                        <a href="<?= $this->escapeHtmlAttr($this->url('register-in-waiting-list', [], ['query' => ['id' => $game->getIdgame()]])) ?>" class="btn btn-warning">
                          S'inscrire en file d'attente
                        </a>
                    </div>  
                  </div>
                  <?php else : ?>
                                      <div class='row'>
                    <div class="col-12 text-center">
                      <div class='alert alert-success'>
                          <p>Vous êtes inscrit en liste d'attente</p>
                      </div>                   
                    </div>
                      <div class="col-12 text-center">
                        <a href="<?= $this->escapeHtmlAttr($this->url('unregister-in-waiting-list', [], ['query' => ['id' => $game->getIdgame()]])) ?>" class="btn btn-warning">
                          Se désinscrire
                        </a>
                    </div>  
                  </div>
              <?php endif ?>
 <?php endif ?>

            <?php else :?>
                <?php if($isComplete): ?>
                    <h4>La prochaine partie est complète!</h4>
                <?php else : ?>    
                    <h4>Prochaine partie du <?= $this->escapeHtml($game->getDate()->format('d/m/Y')) ?> </h4>
                    <?php if($countRegister): ?>
                      <?php if(count($countRegister)>0): ?>  
                      <p>  <?= $this->escapeHtml(count($countRegister)) ?> joueurs inscrits ! </p>
                      <?php endif; ?>
                      <?php endif; ?>
                <?php endif;?>
          <hr>
          <div class='row'>
            <div class="col-12 text-center">
              <?php if($this->currentUser):?>
                  <?php if($this->register): ?>
                    <div class='alert alert-success'>
                      <p>Vous êtes inscrit à la partie</p>
                  </div>
                    <form action="<?= $this->escapeHtmlAttr($this->url('unregister-in-game')) ?>" method="post" style="display: inline;">
                      <input type="hidden" name="id" value="<?= $this->escapeHtmlAttr($register->getIdregister()) ?>">
                      <button type="button" class="btn btn-warning" data-bs-toggle="modal" data-bs-target="#unregisterModal">
                      Se désinscrire
                  </button>
                  </form>
                  <?php else: ?>   


                <div class='row'>
                  <div class="col-12 text-center">
                      <p>Les inscriptions sont ouvertes ! </p>
                  </div>
                    <div class="col-12 text-center">
                      <button type="button" class="btn btn-warning" data-bs-toggle="modal" data-bs-target="#registerModal">
                        S'inscrire
                      </button>
                  </div>  
                </div>
                    
            
                  <?php endif; ?>      
              <?php else: ?>
                  <a href="<?= $this->escapeHtmlAttr($this->url('login')) ?>" class="btn btn-warning">
                      Se connecter
                  </a>        
              <?php endif ?>
            </div>
          </div>
            
            <?php endif ?>


    <?php else: ?>    
        <h4>La prochaine partie n'est pas encore ouverte!</h4>
        <hr>
        <div class='row'>
          <div class="col-12 text-center">
               <p>Elle sera mises en ligne d'ici peu, restez connecter et suivez nous sur les réseaux pour être tenu informer ! </p>
          </div>
        </div>
    <?php endif ?>

</div>

 <?php if (! empty($actus)): ?>
<div class='card'>
    <h4>Les dernières actus ! </h4>
    <hr>

    <?php foreach ($actus as $actu): ?>
      <div class="mb-3">
        <div class="d-flex justify-content-between">
          <div class="fw-bold"><?= $this->escapeHtml($actu->getTitre()) ?></div>
          <?php if ($actu->getDate()): ?>
            <div class="text-muted small"><?= $this->escapeHtml($actu->getDate()->format('d/m/Y')) ?></div>
          <?php endif; ?>
        </div>
  
        <div><?= $this->escapeHtml($actu->getContenu()) ?></div>
        <?php if ($actu->getLien()): ?>
          <div class="mt-2">
            <a href="<?= $this->escapeHtmlAttr($actu->getLien()) ?>" target="_blank" rel="noopener">
              En savoir plus
            </a>
          </div>
        <?php endif; ?>
      </div>
    <?php endforeach; ?>
  <?php else: ?>

</div>
  <?php endif; ?>



<?php if($this->register): ?>

<div class="modal fade" id="unregisterModal" tabindex="-1" aria-labelledby="unregisterModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <form action="<?= $this->escapeHtmlAttr($this->url('unregister-in-game')) ?>" method="post">
        <input type="hidden" name="id" value="<?= $this->escapeHtmlAttr($register->getIdregister()) ?>">
        <div class="modal-header">
          <h5 class="modal-title" id="unregisterModalLabel">Confirmation</h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Fermer"></button>
        </div>
        <div class="modal-body">
          Êtes-vous sûr de vouloir vous désinscrire de cette partie ?
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Annuler</button>
          <button type="submit" class="btn btn-warning">Se désinscrire</button>
        </div>
      </form>
    </div>
  </div>
</div>
<?php endif ;?>

<?php if ($this->currentUser && $game && !$this->register): ?>
<div class="modal fade" id="registerModal" tabindex="-1" aria-labelledby="registerModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <form id="registerConfirmForm" action="<?= $this->escapeHtmlAttr($this->url('register-in-game')) ?>" method="get">
        <input type="hidden" name="id" value="<?= $this->escapeHtmlAttr($game->getIdgame()) ?>">
        <div class="modal-header">
          <h5 class="modal-title" id="registerModalLabel">Confirmation d'inscription</h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Fermer"></button>
        </div>
        <div class="modal-body">
          <p> Les mineurs ne sont passt autorisés à s'inscrire même avec un accompagnant </p>
          <p>Merci de vous présenter à l'accueil entre 8h et 8h30, la grille sera fermée passée cet horaire.</p>
          <p>Nous acceptons les réglementents en espèces et carte bancaire</p>
          <p>Merci d'annuler sur cette page si vous ne pouvez pas venir, cela permettra à d'autres joueurs de s'inscrire !</p>
          <p>Pensez a lire les <a href="<?= $this->escapeHtmlAttr($this->url('faq')) ?>"> <b> conditions et règles présentes dans la FAQ</b></a> avant de confirmer votre inscription !</p>
          <div class="form-check mt-3">
            <input class="form-check-input" type="checkbox" value="1" id="refundConditionsAccepted" required>
            <label class="form-check-label" for="refundConditionsAccepted">
              J'ai lu, compris, et j'accepte les conditions d'inscription.
            </label>
          </div>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Annuler</button>
          <button type="submit" class="btn btn-warning">Confirmer l'inscription</button>
        </div>
      </form>
    </div>
  </div>
</div>
<?php endif; ?>