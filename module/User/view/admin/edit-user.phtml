<?php
$generatedPasswords = $this->flashMessenger()->setNamespace('generatedPassword')->getMessages();
?>
<div class="card mt-4">
    <h4>Modifier un utilisateur</h4>

    <?php if ($currentUser && $currentUser->isInRoles('GOD') && !empty($generatedPasswords)): ?>
        <div class="alert alert-warning d-flex align-items-center justify-content-between mt-3">
            <div>
                Nouveau mot de passe généré : <strong id="generatedPasswordValue"><?= $this->escapeHtml($generatedPasswords[0]) ?></strong>
                <div class="small text-muted">Copiez-le maintenant, il ne sera plus affiché ensuite.</div>
            </div>
            <button type="button" class="btn btn-sm btn-outline-dark" id="btnCopyGeneratedPassword">Copier</button>
        </div>
    <?php endif; ?>

    <form method="post" class="mt-3">
        <div class="row">
            <div class="form-group col-sm-3 mt-3">
                <label for="first_name">Prénom</label>
                <input type="text"
                       class="form-control"
                       name="first_name"
                       id="first_name"
                       value="<?= $this->escapeHtmlAttr($user->getFirstname()) ?>"
                       required>
            </div>

            <div class="form-group col-sm-3 mt-3">
                <label for="last_name">Nom</label>
                <input type="text"
                       class="form-control"
                       name="last_name"
                       id="last_name"
                       value="<?= $this->escapeHtmlAttr($user->getLastname()) ?>"
                       required>        
            </div>  
            <div class="form-group col-sm-3 mt-3">
                <label for="email">Email</label>
                <input type="email"
                       class="form-control"
                       name="email"
                       id="email"
                       value="<?= $this->escapeHtmlAttr($user->getEmail()) ?>"
                       required>
            </div>
            <div class="form-group d-flex justify-content-between mt-3">
                <div class='col-3'>
                    <label for="status">Admin</label>
                    <select class="form-control" name="isAdmin" id="isAdmin">
                        <option value="1" <?= $user->getIsAdmin() == 1 ? 'selected' : '' ?>>Oui </option>
                        <option value="0" <?= $user->getIsAdmin() == 0 ? 'selected' : '' ?>>Non</option>
                    </select>
                </div>
                <div class='col-3'>
                    <label for="status">Membre</label>
                    <select class="form-control" name="isMember" id="isMember">
                        <option value="1" <?= $user->getIsMember() == 1 ? 'selected' : '' ?>>Oui </option>
                        <option value="0" <?= $user->getIsMember() == 0 ? 'selected' : '' ?>>Non</option>
                    </select>
                </div>
                <div class='col-3'>
                    <label for="status">Blacklist</label>
                    <select class="form-control" name="isBlacklist" id="isBlacklist">
                        <option value="1" <?= $user->getIsBlacklist() == 1 ? 'selected' : '' ?>>Oui </option>
                        <option value="0" <?= $user->getIsBlacklist() == 0 ? 'selected' : '' ?>>Non</option>
                    </select>
                </div>
        
            </div>
        </div>

        <div class='d-flex justify-content-between mt-5 '>
            <div class="form-group ">
                <button type="submit" class="btn btn-warning">Enregistrer</button>
                <a href="<?= $this->escapeHtmlAttr($this->url('admin-users')) ?>" class="btn btn-secondary">Annuler</a>
            </div>
        </form>
            <div class="form-group">
                <form method="post" action="<?= $this->escapeHtmlAttr($this->url('admin-delete-user')) ?>" class="delete-form ">
                    <input type="hidden" name="iduser" value="<?= $this->escapeHtmlAttr($user->getIdUser()) ?>">
                    <button type="button" class="btn btn-danger btn-delete" data-iduser="<?= $this->escapeHtmlAttr($user->getIdUser()) ?>">
                            Supprimer
                    </button>
                </form>
            </div>
        </div>

        <div class="d-flex mt-3">
            <div class="form-group">
                <form method="post" action="<?= $this->escapeHtmlAttr($this->url('admin-send-reset-password')) ?>" class="reset-password-form">
                    <input type="hidden" name="iduser" value="<?= $this->escapeHtmlAttr($user->getIdUser()) ?>">
                    <button type="button" class="btn btn-outline-primary btn-reset-password" data-iduser="<?= $this->escapeHtmlAttr($user->getIdUser()) ?>">
                            Envoyer un lien de réinitialisation
                    </button>
                </form>
            </div>
            <?php if ($currentUser && $currentUser->isInRoles('GOD')): ?>
            <div class="form-group ms-2">
                <form method="post" action="<?= $this->escapeHtmlAttr($this->url('admin-generate-password')) ?>" class="generate-password-form">
                    <input type="hidden" name="iduser" value="<?= $this->escapeHtmlAttr($user->getIdUser()) ?>">
                    <button type="button" class="btn btn-outline-dark btn-generate-password" data-iduser="<?= $this->escapeHtmlAttr($user->getIdUser()) ?>">
                            Générer un nouveau mot de passe
                    </button>
                </form>
            </div>
            <?php endif; ?>
        </div>
</div>




<div class="modal fade" id="confirmDeleteModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <form method="post" id="confirmDeleteForm">
                <div class="modal-header">
                <h5 class="modal-title">Confirmation</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Fermer"></button>
                </div>
                <div class="modal-body">
                Êtes-vous sûr de vouloir désactiver ce joueur ?
                </div>
                <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Annuler</button>
                <button type="submit" class="btn btn-danger">Confirmer</button>
                </div>
            </form>
        </div>
    </div>
</div>

<div class="modal fade" id="confirmResetModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <form method="post" id="confirmResetForm">
                <div class="modal-header">
                <h5 class="modal-title">Confirmation</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Fermer"></button>
                </div>
                <div class="modal-body">
                Envoyer un email de réinitialisation de mot de passe à cet utilisateur ?
                </div>
                <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Annuler</button>
                <button type="submit" class="btn btn-primary">Confirmer</button>
                </div>
            </form>
        </div>
    </div>
</div>

<?php if ($currentUser && $currentUser->isInRoles('GOD')): ?>
<div class="modal fade" id="confirmGenerateModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <form method="post" id="confirmGenerateForm">
                <div class="modal-header">
                <h5 class="modal-title">Confirmation</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Fermer"></button>
                </div>
                <div class="modal-body">
                Générer un nouveau mot de passe pour cet utilisateur ? L'ancien mot de passe ne fonctionnera plus.
                </div>
                <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Annuler</button>
                <button type="submit" class="btn btn-dark">Confirmer</button>
                </div>
            </form>
        </div>
    </div>
</div>
<?php endif; ?>

<script src="/js/pages/admin/edit-user.js"></script>