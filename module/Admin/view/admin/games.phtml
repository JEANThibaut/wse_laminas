<h4>Ajouter une partie</h4>

<form method="post">
    <div class="row">
        <div class="form-group col-sm-3">
            <label for="date">Date</label>
            <input type="date" name="date" id="date" class="form-control datepicker" placeholder="JJ/MM/AAAA" required>
        </div>

        <div class="form-group col-sm-3">
            <label for="player_max">Joueurs max</label>
            <input type="number" name="player_max" id="player_max" class="form-control" required>
        </div>

        <div class="form-group col-sm-3">
            <label for="status">Statut</label>
            <select name="status" id="status" class="form-select" required>
                <option value="1">Ouverte</option>
                <option value="0">Fermée</option>
            </select>
        </div>

        <div class="form-group col-sm-3 d-flex align-items-end mt-5">
            <button type="submit" class="btn btn-primary w-50">Valider</button>
        </div>
    </div>
</form>

<table id="gamesTable" class="table table-bordered table-striped mt-5">
    <thead>
        <tr>
            
            <th>Date</th>
            <th>Joueurs max</th>
            <th>Statut</th>
            <th>Action</th>
        </tr>
    </thead>
    <tbody>
        <?php foreach ($games as $game): ?>
            <tr>
                <td><?= $game->getDate()->format('d/m/Y') ?></td>
                <td><?= $this->escapeHtml($game->getPlayerMax()) ?></td>
                <td><?= $game->getStatus() ? 'Actif' : 'Inactif' ?></td>
                <td>
                    <a href="<?= $this->url('admin-edit-game', ['id' => $game->getIdgame()]) ?>" class="btn btn-sm btn-warning">
                        Modifier
                    </a>
                    <form method="post" action="<?= $this->url('admin-delete-game') ?>" style="display: inline;" onsubmit="return confirm('Confirmer la suppression ?');">
                        <input type="hidden" name="id" value="<?= $game->getIdgame() ?>">
                        <button type="submit" class="btn btn-sm btn-danger">Supprimer</button>
                    </form>
                </td>
            </tr>
        <?php endforeach; ?>
    </tbody>
</table>
